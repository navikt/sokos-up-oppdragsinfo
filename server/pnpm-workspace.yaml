# pnpm settings for the server/ project (independent from root install).
# Overrides here apply ONLY to `cd server && pnpm install`.
# Root overrides live in ../pnpm-workspace.yaml and are NOT inherited.

overrides:
  qs: 6.15.2
allowBuilds:
  esbuild: true
